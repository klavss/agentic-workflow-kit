---
name: security-baseline
description: Use for standardized security risk checks on auth, secrets, env handling, network access, permissions, data exposure, and deploy surfaces. Trigger when sensitive surfaces are touched or Harden/Release mode is active. Do not use for feature implementation. This skill is single-purpose: security baseline assessment only.
---

# Purpose
Run baseline security checks and report P0-P3 mitigations.
